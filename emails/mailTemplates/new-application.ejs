<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New EEP Application</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
            color: #1e293b;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .container {
            width: 100%;
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            padding: 40px;
            border-radius: 24px;
            box-shadow: 0 10px 25px rgba(59, 130, 246, 0.1);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
            padding: 30px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            border-radius: 16px;
            color: white;
        }

        h1 {
            font-size: 28px;
            margin: 0;
            font-weight: 700;
            letter-spacing: -0.5px;
        }

        .sub-header {
            color: rgba(255, 255, 255, 0.9);
            font-size: 16px;
            margin-top: 10px;
        }

        .section {
            background-color: #f8fafc;
            padding: 24px;
            border-radius: 16px;
            margin-bottom: 24px;
            border: 1px solid rgba(59, 130, 246, 0.1);
        }

        .section-title {
            font-size: 18px;
            color: #3b82f6;
            margin-bottom: 12px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .section-content {
            font-size: 16px;
            color: #334155;
        }

        .section-content a {
            color: #3b82f6;
            text-decoration: none;
            font-weight: 500;
        }

        .section-content a:hover {
            text-decoration: underline;
        }

        .interests-section {
            background-color: #ffffff;
            border: 1px solid rgba(59, 130, 246, 0.2);
        }

        .cv-section {
            background-color: #f0f9ff;
            border: 1px dashed #3b82f6;
        }

        .footer {
            text-align: center;
            font-size: 14px;
            color: #64748b;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
        }

        .footer p {
            margin: 0;
        }

        .badge {
            display: inline-block;
            padding: 4px 12px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            color: white;
            border-radius: 9999px;
            font-size: 14px;
            font-weight: 500;
        }

        @media (max-width: 600px) {
            .container {
                padding: 24px;
                margin: 20px;
            }

            h1 {
                font-size: 24px;
            }

            .section {
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>New EEP Application 🚀</h1>
            <p class="sub-header">A new candidate has applied for the Enterprise Engagement Program</p>
        </div>

        <div class="section">
            <div class="section-title">👤 Applicant Name</div>
            <div class="section-content">
                <%= applicant.fullName %>
            </div>
        </div>

        <div class="section">
            <div class="section-title">📧 Email Address</div>
            <div class="section-content">
                <a href="mailto:<%= applicant.email %>">
                    <%= applicant.email %>
                </a>
            </div>
        </div>

        <div class="section">
            <div class="section-title">📱 Phone Number</div>
            <div class="section-content">
                <%= applicant.phone %>
            </div>
        </div>

        <div class="section interests-section">
            <div class="section-title">💡 Areas of Interest</div>
            <div class="section-content">
                <%= applicant.interests %>
            </div>
        </div>

        <div class="footer">
            <p>Next Steps:</p>
            <p style="margin-top: 8px; color: #3b82f6;">Review application and schedule initial assessment</p>
            <p style="margin-top: 20px; font-size: 12px; color: #94a3b8;">
                Enterprise Engagement Program | <%= new Date().toLocaleDateString() %>
            </p>
        </div>
    </div>
</body>

</html>